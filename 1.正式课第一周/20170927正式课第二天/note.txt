1.预解释变态机制
   1.在条件语句中，不管条件是否成立，都要进行预解释,function在最新版本浏览器中，只声明不定义，在老版本浏览器，不仅声明，还定义；
   2.等号右边的不进行预解释
   3.return 出去的内容是不进行预解释的,return 下面的代码是需要进行预解释的
   4.匿名函数不需要进行预解释；
   5.如果变量名字重复，变量只声明一次,但是会重新定义(function)；